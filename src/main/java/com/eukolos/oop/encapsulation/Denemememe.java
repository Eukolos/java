package com.eukolos.oop.encapsulation;

// import com.eukolos.oop.encapsulation.encapsulated.Deneme;

// Deneme class is package-private, so it can be accessed only from the same package
// and can not extend from another package

/*
public class Denemememe extends Deneme {


    public Denemememe(String name, String surname) {
        super(name, surname);
    }

    public static void main(String[] args) {
        Deneme deneme = new Deneme("Eukolos", "Eukolos");
        System.out.println(deneme.getName());
    }
}
*/